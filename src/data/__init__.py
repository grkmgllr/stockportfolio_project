"""New canonical data pipeline (multi-ticker, cross-sectional aware).

Kept intentionally separate from the legacy ``src/dataset.py`` /
``src/forecasting/data_loading.py`` path used by the 5-ticker baseline.
"""
